<UserControl x:Class="Jaywapp.Toasket.View.List.BoxItemListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" d:DesignHeight="450" d:DesignWidth="800">
    <ScrollViewer >
        <ListView ItemsSource="{Binding Items}" SelectedItem="{Binding SelectedItem}">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Created" DisplayMemberBinding="{Binding Box.Created}"/>
                    <GridViewColumn Header="Ratio" Width="100" DisplayMemberBinding="{Binding Ratio}"/>
                    <GridViewColumn Header="Money" Width="100" DisplayMemberBinding="{Binding Money}"/>
                    <GridViewColumn Header="ReturnMoney" Width="100" DisplayMemberBinding="{Binding ReturnMoney}"/>
                </GridView>
            </ListView.View>
        </ListView>
    </ScrollViewer>
</UserControl>
